# Example .env for local development - do NOT commit your real .env to git
# Copy this file to `.env` and fill in the values locally.

# Emailing (used by the app's Email sidebar feature)
EMAIL_FROM=you@example.com
EMAIL_TO=recipient@example.com
EMAIL_PASSWORD=your_app_password_here
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Optional: RSS / external links
ALERTS_SITE_URL=http://localhost:8501/

# App settings (example)
DEBUG=False

# NOTE: Add `.env` to your `.gitignore` to avoid committing secrets.
